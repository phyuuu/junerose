export type CartItem = {
  variantId: number;
  productId: number;
  slug: string;
  name: string;
  priceMMK: number;
  image: string;
  selectedSize: string;
  selectedColor: string;
  quantity: number;
};
